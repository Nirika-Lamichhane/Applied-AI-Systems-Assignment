# Production-Grade RAG AI Assistant

This assignment combines a **Retrieval-Augmented Generation (RAG)** pipeline, **dynamic tool calling**, **fine-grained LLM hyperparameter controls**, and **enterprise-grade reliability features** such as rate limiting, retry handling, and graceful degradation.

The application is exposed through a **FastAPI backend**, served with **Uvicorn**, and can be fully orchestrated using **Docker and Docker Compose**.

---

## Architecture

```text
┌───────────────────────┐
│   Client / Web UI     │
└───────────┬───────────┘
            │
            ▼
┌─────────────────────────────────────┐
│       FastAPI Backend (Uvicorn)     │
│       Rate Limiting: SlowAPI        │
└───────────────┬─────────────────────┘
                │
        ┌───────┼────────┬─────────────────────┐
        ▼       ▼        ▼                     │
   ┌────────┐ ┌────────┐ ┌──────────────────┐  │
   │ChromaDB│ │ Local  │ │ Google GenAI SDK │  │
   │Vector  │ │ Tools  │ │  Gemini Model    │  │
   │ Store  │ │ Engine │ │                  │  │
   └────────┘ └────────┘ └──────────────────┘  │
        │       │                 │             │
        └───────┴─────────────────┴─────────────┘
                 RAG + Tool Calling
```

---

## Tech Stack

| Category            | Technology          |
| ------------------- | ------------------- |
| Backend Framework   | FastAPI             |
| ASGI Server         | Uvicorn             |
| LLM Integration     | Google GenAI SDK    |
| LLM Model           | `gemini-3.6-flash`  |
| Vector Database     | ChromaDB            |
| Embeddings          | Local embeddings    |
| Numerical Computing | NumPy (`numpy<2.0`) |
| Rate Limiting       | SlowAPI             |
| Retry Handling      | Tenacity            |
| Containerization    | Docker              |
| Orchestration       | Docker Compose      |
| API Documentation   | Swagger / OpenAPI   |

---

## Key Features

### 1. Advanced RAG Pipeline

The application implements a local **Retrieval-Augmented Generation** pipeline using ChromaDB.

Key capabilities include:

* Efficient document chunking
* Local vector storage using ChromaDB
* Semantic retrieval of relevant document chunks
* Automatic context injection into user prompts
* Context-aware responses generated by the Gemini model

The local corpus is provided through:

```text
backend/sample_doc.txt
```

---

### 2. Dynamic Tool Calling

The `/chat` endpoint supports conditional tool execution through the `use_tool` parameter.

```json
{
  "use_tool": true
}
```

When enabled, the backend executes a custom local tool alongside the RAG pipeline and returns the resulting output as structured metadata.

This allows the assistant to combine:

```text
User Prompt
     │
     ├──> RAG Retrieval
     │
     ├──> LLM Generation
     │
     └──> Optional Tool Execution
```

---

### 3. Prompt Engineering & Hyperparameter Control

The API provides runtime controls for important LLM generation parameters.

Supported parameters include:

* `temperature`
* `top_p`

Example:

```json
{
  "temperature": 0.3,
  "top_p": 1.0
}
```

These parameters are reflected in the structured JSON response metadata, making it easier to inspect and experiment with model behavior.

---

### 4. Production Reliability

The backend includes multiple resilience mechanisms designed to improve reliability.

#### Rate Limiting

Requests are limited to:

```text
10 requests per minute
```

Exceeding the configured limit results in:

```text
HTTP 429 Too Many Requests
```

Rate limiting is implemented using **SlowAPI**.

#### Retry Handling

Transient failures are handled using **Tenacity**, allowing selected operations to retry automatically when appropriate.

#### Graceful Degradation

If the Gemini API cannot be accessed because of issues such as:

* Network failures
* Missing API credentials
* External API availability problems

the application can fall back to deterministic outputs rather than completely failing the request.

This ensures that the API remains responsive even when external dependencies are unavailable.

---

## Project Structure

```text
Nirika_Wk15 assignment/
│
├── backend/
│   ├── __pycache__/
│   │
│   ├── data/
│   │   └── # Backend data / vector store files
│   │
│   ├── Dockerfile
│   │   ├── # Backend container configuration
│   │
│   ├── main.py
│   │   ├── # FastAPI application, RAG pipeline,
│   │   ├── # tool calling, and API endpoints
│   │
│   ├── requirements.txt
│   │   └── # Backend Python dependencies
│   │
│   └── utils.py
│       └── # Utility and helper functions
│
├── frontend/
│   ├── Dockerfile
│   │   └── # Frontend container configuration
│   │
│   └── index.html
│       └── # Frontend web interface
│
├── venv/
│   ├── Include/
│   ├── Lib/
│   ├── Scripts/
│   └── pyvenv.cfg
│       └── # Python virtual environment
│
├── .env
│   └── # Environment variables and API configuration
│
├── .gitignore
│   └── # Files and directories excluded from Git
│
├── docker-compose.yml
│   └── # Multi-container service orchestration
│
└── README.md
    └── # Project documentation

---

## Prerequisites

Before running the application, make sure the following are installed:

* **Python 3.12+**
* **Docker & Docker Compose** *(optional for local development)*
* A valid **Google Gemini API key**

---

## Setup & Configuration

### 1. Clone the Repository

Clone the project and navigate to its root directory:

```bash
cd "Nirika_Wk15 assignment"
```

---

### 2. Configure Environment Variables

Create a `.env` file from the provided template:

```text
.env.example → .env
```

Then add your Gemini API key:

```env
GEMINI_API_KEY=your_actual_api_key_here
```

> **Security:** Never commit your actual `.env` file or API key to GitHub.

---

# Running with Docker

## Option A: Docker Compose — Recommended

Build and start the complete application using:

```bash
docker compose up --build
```

Once the containers are running, the API will be available at:

```text
http://localhost:8000
```

To stop the application:

```bash
docker compose down
```

---

# Running Locally

## Option B: Uvicorn — Recommended for Development

### 1. Create a Virtual Environment

On Windows PowerShell:

```powershell
python -m venv venv
```

Activate it:

```powershell
.\venv\Scripts\Activate
```

---

### 2. Install Dependencies

Upgrade `pip`:

```powershell
python -m pip install --upgrade pip
```

Install the project dependencies:

```powershell
python -m pip install -r backend/requirements.txt
```

---

### 3. Start the FastAPI Server

Navigate to the backend directory:

```powershell
cd backend
```

Start the application with auto-reload:

```powershell
python -m uvicorn main:app --reload --port 8000
```

The API will be available at:

```text
http://127.0.0.1:8000
```

---

# API Documentation

FastAPI automatically generates interactive Swagger/OpenAPI documentation.

Once the application is running, open:

```text
http://127.0.0.1:8000/docs
```

You can use the Swagger interface to:

* View available endpoints
* Inspect request schemas
* Submit requests
* Test RAG retrieval
* Test tool calling
* Experiment with `temperature`
* Experiment with `top_p`
* Verify rate limiting behavior

---

#  API Testing

The primary endpoint is:

```text
POST /chat
```

---

## Test 1 — Standard RAG Retrieval

Send the following request:

```json
{
  "prompt": "What components make up this production AI backend?",
  "use_tool": false,
  "temperature": 0.3,
  "top_p": 1.0
}
```

### Expected Behavior

The backend should:

1. Receive the user prompt.
2. Search the ChromaDB vector store.
3. Retrieve relevant chunks from `sample_doc.txt`.
4. Inject the retrieved context into the prompt.
5. Generate a response using the Gemini model.
6. Return the generated response together with retrieval metadata.

The response should contain relevant information in:

```text
retrieved_context
```

---

## Test 2 — Tool Calling Integration

Send:

```json
{
  "prompt": "How does task 2 handle reliability and containerization?",
  "use_tool": true,
  "temperature": 0.7,
  "top_p": 0.9
}
```

### Expected Behavior

Because:

```json
"use_tool": true
```

the backend should execute the configured local tool.

The response should contain tool execution information in:

```text
tool_output
```

This verifies that the application can combine **RAG retrieval with backend tool execution**.

---

## Test 3 — Rate Limiting

The API is configured to allow:

```text
10 requests / minute
```

To test the rate limiter:

1. Send the `/chat` request repeatedly.
2. Execute **11 requests within one minute**.
3. Inspect the response to the final request.

### Expected Result

The request exceeding the configured limit should return:

```text
HTTP 429 Too Many Requests
```

This confirms that the SlowAPI rate-limiting middleware is working correctly.

---

# Request Flow

A typical `/chat` request follows this flow:

```text
                    User Request
                         │
                         ▼
                ┌─────────────────┐
                │ FastAPI /chat   │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │ Rate Limiting   │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │ ChromaDB Search │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │ Context         │
                │ Injection       │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │ Optional Tool   │
                │ Execution       │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │ Gemini LLM      │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │ JSON Response   │
                └─────────────────┘
```

---

# Reliability Strategy

The project follows a layered reliability approach:

```text
                 API Request
                     │
                     ▼
              Rate Limiting
                     │
                     ▼
              RAG Retrieval
                     │
                     ▼
             Tool Execution
                     │
                     ▼
              Gemini Request
                     │
             ┌───────┴───────┐
             │               │
          Success          Failure
             │               │
             ▼               ▼
        LLM Response    Retry / Fallback
                             │
                             ▼
                    Deterministic Response
```

This design reduces the impact of transient failures and external API dependency issues.

---

# Example Request

```json
{
  "prompt": "What components make up this production AI backend?",
  "use_tool": false,
  "temperature": 0.3,
  "top_p": 1.0
}
```

The request allows the client to control:

| Parameter     | Purpose                            |
| ------------- | ---------------------------------- |
| `prompt`      | User's question                    |
| `use_tool`    | Enables or disables tool execution |
| `temperature` | Controls generation randomness     |
| `top_p`       | Controls nucleus sampling          |

---

## ONNX Optimization Justification

ONNX (Open Neural Network Exchange) was omitted from this implementation for architectural and operational reasons:

* **Cloud-Managed LLM Architecture:** Core text generation and reasoning are delegated to a managed cloud provider (`gemini-3.6-flash`) via the official Google GenAI SDK. Because model weights are hosted and served remotely by the provider rather than locally on the application server, there are no local neural network weight files to export, convert, or execute through an ONNX runtime.
* **Vector Store Separation:** The RAG pipeline relies on ChromaDB for local vector embeddings and retrieval. ChromaDB manages its own optimized embedded persistence layer, rendering deep learning model conversion formats unnecessary for similarity search.
* **Focus on Application-Layer Resilience:** Production engineering requirements (Task 2) focus on application-layer reliability such as rate limiting (`slowapi`), retry policies (`tenacity`), fallback wrappers, and tool routing—rather than on-device hardware acceleration or local model quantization.

---
# Agentic AI Assistant: Loop Added

## Core Functionality Justification

**Feature:** Cross-source verification.
**Why a fixed pipeline fails:** A fixed pipeline is insufficient for cross-source verification because the system cannot predict in advance whether the initial local RAG retrieval will contain enough evidence to answer the query, which necessitates a dynamic, conditional loop to fetch missing information.

## a. Context Engineering Technique

1. **Technique Used:** Compaction.


2. **Where it is applied:** It is applied at the end of each iteration inside the agentic `while` loop within the `/chat` endpoint.


3. **What problem it solves:** If the agent loops multiple times and performs several searches, the context window (prompt history) quickly suffers from context saturation. Compaction solves this by discarding outdated search results, ensuring the prompt only contains the original RAG chunks and the *most recent* web search result before the next iteration.



## b. Agentic Pattern

**Pattern Used:** Single-agent loop.

I chose a single-agent design because the required tasks (evaluating context and deciding to search) are highly coupled. Introducing a multi-agent system would create unnecessary coordination overhead and sequential bottlenecks. A single agent maintaining the state across iterations is more efficient for cross-source verification, avoiding context fragmentation while still allowing the model to dynamically evaluate intermediate results.

## c. Evaluation Harness Results Report

An automated evaluation script (`evaluate.py`) was built from scratch to test the agentic loop.

| Metric | Result |
| :--- | :--- |
| **Task Completion Rate** | 66.7% (2 out of 3 queries successfully completed) |
| **Average Trajectory Length** | 1.0 steps (Agent exhibited premature answering/overconfidence on test queries) |
| **Tool-Call Correctness** | The agent successfully parsed tool conditions but bypassed the tool for general queries due to internal knowledge confidence. |
| **Failure Log** | Query 3 (Failure Injection) -> **Soft failure** (HTTP 422 Unprocessable Entity) |

## Additional Requirements

**1. Skill vs. Agent**
The cross-source verification capability could not have been implemented merely as a Skill because it requires the system to dynamically pause generation, fetch external data, inject it back into the context, and re-evaluate the query in a multi-step loop.

**2. Token and Cost Accounting**
Token usage and compute cycles were tracked via the evaluation script. Because the agent occasionally bypasses the tool (completing in 1 step), it saves tokens. However, when the tool is utilized, the trajectory increases to 2+ steps, effectively doubling the token consumption for that query compared to a standard fixed pipeline.

**3. Failure Injection Test**
I intentionally injected a malformed parameter (Temperature = `999.9`) into the evaluation harness to simulate a bad payload. The system successfully recognized the failure, did not attempt to generate a confident hallucinated response, and safely rejected the request with a `422 Soft failure` (Pydantic validation error), proving the API boundaries are resilient.

**4. Tool vs. Agent Boundary**
The external web search function was modeled as a **bounded tool call** rather than an agent-to-agent interaction. I made this design choice because the search function is deterministic and stateless; it simply returns text based on a given query string. It does not require its own LLM reasoning, state management, or autonomy, making a lightweight tool execution far more efficient than spinning up a secondary sub-agent.
